import React from 'react';
import ImageToPixelBoard from '../features/ImageToPixelBoard';

// This is a simple wrapper component to make it easier to use in the router
const ImageToPixelBoardPage: React.FC = () => {
	return <ImageToPixelBoard />;
};

export default ImageToPixelBoardPage;
